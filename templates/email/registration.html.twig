{# email translation here email.registration is trans-unit ID which is 
in messages.en and messages.fr file #}

{# and here %name%, %link% etc variables which is defined for set email.
which I call in email body
#}

{# row is defind for email format and avoid extra space #}

{{ 'email.registration'|trans({
    '%name%': user.fullName,
    '%confirm-link%': url('security_confirm', {'token': user.confirmationToken}),
    '%login-link%': url('security_login')
})|raw }}